import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    card: {
        borderRadius: 28,
        margin: 10,
        overflow: 'hidden',
        elevation: 7,
        height: '20%',
    },
    pressed: {
        opacity: 0.75,
    },
});
